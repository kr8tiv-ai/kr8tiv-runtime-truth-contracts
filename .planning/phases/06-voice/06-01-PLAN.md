---
phase: 06-voice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - voice/kokoro-tts.ts
  - voice/kokoro-types.ts
  - voice/local-tts.ts
  - voice/pipeline.ts
  - voice/index.ts
  - tests/kokoro-tts.test.ts
autonomous: true
requirements: [VOICE-01, VOICE-04]

must_haves:
  truths:
    - "Kokoro TTS synthesizes speech from text using ONNX runtime on CPU with no GPU dependency"
    - "Each companion has a distinct KokoClone voice embedding loaded from voice/profiles/{companionId}.bin"
    - "Kokoro replaces XTTS as primary local TTS provider while Piper remains as ultra-low-resource fallback"
    - "VoicePipeline.synthesize with provider=local routes to Kokoro instead of XTTS"
  artifacts:
    - path: "voice/kokoro-types.ts"
      provides: "Type definitions for Kokoro TTS integration and voice embeddings"
      exports: ["KokoroConfig", "KokoroVoiceEmbedding", "CompanionVoiceMap"]
    - path: "voice/kokoro-tts.ts"
      provides: "Kokoro ONNX TTS synthesis with KokoClone voice embedding support"
      contains: "synthesizeWithKokoro"
    - path: "voice/local-tts.ts"
      provides: "Updated local TTS with Kokoro as primary, Piper as fallback"
      contains: "kokoro"
    - path: "tests/kokoro-tts.test.ts"
      provides: "Unit tests for Kokoro TTS integration"
      contains: "synthesizeWithKokoro"
  key_links:
    - from: "voice/kokoro-tts.ts"
      to: "voice/kokoro-types.ts"
      via: "imports KokoroConfig, KokoroVoiceEmbedding"
      pattern: "import.*from.*kokoro-types"
    - from: "voice/local-tts.ts"
      to: "voice/kokoro-tts.ts"
      via: "synthesizeWithKokoro call replaces synthesizeWithXtts as primary"
      pattern: "synthesizeWithKokoro"
    - from: "voice/pipeline.ts"
      to: "voice/local-tts.ts"
      via: "synthesizeLocal delegates to updated local-tts"
      pattern: "synthesizeLocalTts"
---

<objective>
Replace XTTS v2 with Kokoro TTS (82M params, MOS 4.2) as the primary local TTS engine and add per-companion KokoClone voice embeddings.

Purpose: Kokoro is 20x smaller than XTTS v2 (82M vs 1.6GB), runs on CPU with RTF ~0.2, and produces higher quality speech (MOS 4.2 vs 4.0). KokoClone enables unique voice identity per companion bloodline without model retraining — just a few-KB embedding file per companion.

Output: Working Kokoro TTS integration with 6 companion voice embedding slots, callable through existing VoicePipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md (Kokoro section — 82M params, kokoro-onnx, KokoClone, RTF 0.03 GPU / ~0.2 CPU)

<interfaces>
<!-- Existing voice pipeline interfaces the executor needs -->

From voice/pipeline.ts:
```typescript
export interface VoiceConfig {
  ttsProvider?: 'elevenlabs' | 'openai' | 'local';
  elevenLabsApiKey?: string;
  openAiApiKey?: string;
  defaultVoice?: string;
  outputFormat?: 'mp3' | 'opus' | 'aac' | 'flac';
  maxDurationSeconds?: number;
}

export interface SynthesisResult {
  audioBuffer: Buffer;
  durationSeconds: number;
  format: string;
  voiceId: string;
}

export interface VoicePersonality {
  companionId: string;
  voiceId: string;
  style: string;
  speed: number;
  pitch: number;
  characteristics: string[];
}
```

From voice/local-tts.ts:
```typescript
export interface LocalTtsConfig {
  provider?: 'xtts' | 'piper';
  xtts?: XttsConfig;
  piper?: PiperConfig;
  profilesDir?: string;
}

export async function synthesizeLocalTts(text: string, companionId: string, config?: LocalTtsConfig): Promise<SynthesisResult>;
export async function isXttsAvailable(serverUrl?: string): Promise<boolean>;
export async function isPiperAvailable(): Promise<boolean>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Kokoro TTS types and synthesis engine</name>
  <files>voice/kokoro-types.ts, voice/kokoro-tts.ts, tests/kokoro-tts.test.ts</files>
  <read_first>voice/pipeline.ts, voice/local-tts.ts, voice/index.ts</read_first>
  <behavior>
    - synthesizeWithKokoro returns SynthesisResult with audioBuffer, format "wav", voiceId "kokoro_{companionId}"
    - synthesizeWithKokoro throws VoicePipelineError("KOKORO_NOT_AVAILABLE") when kokoro-onnx server is unreachable
    - synthesizeWithKokoro loads companion-specific voice embedding from profilesDir/{companionId}.bin
    - synthesizeWithKokoro throws VoicePipelineError("MISSING_VOICE_EMBEDDING") when embedding file missing
    - isKokoroAvailable returns true when Kokoro ONNX server responds at configured URL
    - isKokoroAvailable returns false on timeout/connection refused
    - COMPANION_KOKORO_VOICES maps all 6 companions to voice descriptors matching CONTEXT.md personalities
  </behavior>
  <action>
    Create voice/kokoro-types.ts with:
    - KokoroConfig interface: serverUrl (default "http://localhost:5100"), language ("en"), timeoutMs (15000), modelId (default "kokoro-v1.0")
    - KokoroVoiceEmbedding interface: companionId, embeddingPath, description, pacing ("slow"|"measured"|"quick"), energy ("calm"|"focused"|"elevated")
    - CompanionVoiceMap: Record<string, KokoroVoiceEmbedding> with all 6 companions matching CONTEXT.md voice descriptions:
      - cipher: "Playful, slightly lower register, quick when excited" pacing=quick, energy=elevated
      - forge: "Warm gentle tenor, patient pace" pacing=measured, energy=focused
      - vortex: "Confident authoritative baritone, measured when strategizing" pacing=measured, energy=calm
      - mischief: "Warm energetic mid-range, quick when excited" pacing=quick, energy=elevated
      - aether: "Deep resonant baritone, slow thoughtful pace" pacing=slow, energy=calm
      - catalyst: "Warm shifting quality, adapts pace" pacing=measured, energy=focused

    Create voice/kokoro-tts.ts with:
    - synthesizeWithKokoro(text, companionId, config?): POST to Kokoro ONNX server at {serverUrl}/v1/audio/speech with JSON body { text, voice_embedding: base64(embedding), speed: pacing_to_speed_map }. Read embedding from {profilesDir}/{companionId}.bin. Return SynthesisResult.
    - isKokoroAvailable(serverUrl?): GET {serverUrl}/health with 3s timeout, returns boolean.
    - Speed mapping: slow=0.85, measured=1.0, quick=1.15

    Write tests first (RED) then implement (GREEN). Mock fetch and fs.readFile.
  </action>
  <acceptance_criteria>
    - grep -q "KokoroConfig" voice/kokoro-types.ts
    - grep -q "KokoroVoiceEmbedding" voice/kokoro-types.ts
    - grep -q "cipher.*quick" voice/kokoro-types.ts
    - grep -q "aether.*slow" voice/kokoro-types.ts
    - grep -q "synthesizeWithKokoro" voice/kokoro-tts.ts
    - grep -q "isKokoroAvailable" voice/kokoro-tts.ts
    - grep -q "KOKORO_NOT_AVAILABLE" voice/kokoro-tts.ts
    - grep -q "MISSING_VOICE_EMBEDDING" voice/kokoro-tts.ts
    - grep -q "synthesizeWithKokoro" tests/kokoro-tts.test.ts
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/kokoro-tts.test.ts</automated>
  </verify>
  <done>Kokoro TTS engine created with typed config, 6 companion voice maps, embedding-based synthesis, and passing tests</done>
</task>

<task type="auto">
  <name>Task 2: Wire Kokoro into local-tts and pipeline as primary local provider</name>
  <files>voice/local-tts.ts, voice/pipeline.ts, voice/index.ts</files>
  <read_first>voice/kokoro-tts.ts, voice/kokoro-types.ts, voice/local-tts.ts, voice/pipeline.ts, voice/index.ts</read_first>
  <action>
    Update voice/local-tts.ts:
    - Add provider option 'kokoro' to LocalTtsConfig: provider?: 'kokoro' | 'xtts' | 'piper'
    - Add kokoro?: KokoroConfig to LocalTtsConfig
    - Update synthesizeLocalTts to try Kokoro FIRST, then XTTS as secondary, then Piper as fallback:
      1. isKokoroAvailable() -> synthesizeWithKokoro()
      2. isXttsAvailable() -> synthesizeWithXtts() (kept for backward compat)
      3. isPiperAvailable() -> synthesizeWithPiper()
    - Export isKokoroAvailable from this module
    - Keep all existing XTTS and Piper code intact (backward compat)

    Update voice/pipeline.ts:
    - Add 'kokoro' to VoiceConfig.ttsProvider union: 'elevenlabs' | 'openai' | 'local' | 'kokoro'
    - In VoicePipeline.synthesize, add 'kokoro' case that calls synthesizeWithKokoro directly
    - Update synthesizeLocal to use new fallback chain (kokoro -> xtts -> piper -> openai)
    - Default LOCAL_TTS_PROVIDER env var to 'kokoro' instead of 'xtts'

    Update voice/index.ts:
    - Export synthesizeWithKokoro, isKokoroAvailable, KokoroConfig, KokoroVoiceEmbedding, CompanionVoiceMap from kokoro-tts and kokoro-types
    - Keep all existing exports intact

    Update api/routes/voice.ts provider check:
    - In the 'local' provider preflight, add isKokoroAvailable() to the availability check alongside xtts and piper
  </action>
  <acceptance_criteria>
    - grep -q "kokoro" voice/local-tts.ts
    - grep -q "isKokoroAvailable" voice/local-tts.ts
    - grep -q "kokoro" voice/pipeline.ts
    - grep -q "synthesizeWithKokoro" voice/index.ts
    - grep -q "KokoroConfig" voice/index.ts
    - grep -q "isKokoroAvailable" voice/index.ts
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/kokoro-tts.test.ts && npx vitest run tests/voice-conversation.test.ts</automated>
  </verify>
  <done>Kokoro is the primary local TTS provider. Pipeline falls back to XTTS then Piper. All existing tests still pass. New exports available from voice/index.ts.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/kokoro-tts.test.ts` — Kokoro TTS tests pass
2. `npx vitest run tests/voice-conversation.test.ts` — Existing voice conversation tests still pass
3. `grep -c "companionId" voice/kokoro-types.ts` returns 6+ (all companions mapped)
4. `npx tsc --noEmit` — No type errors introduced
</verification>

<success_criteria>
- Kokoro TTS engine is callable via synthesizeWithKokoro() with per-companion voice embeddings
- VoicePipeline with provider=local tries Kokoro first, falls back to XTTS then Piper
- All 6 companion voice personality descriptors match CONTEXT.md specifications
- Existing ElevenLabs and OpenAI TTS paths are unmodified
- All existing tests pass, new Kokoro tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-voice/06-01-SUMMARY.md`
</output>
