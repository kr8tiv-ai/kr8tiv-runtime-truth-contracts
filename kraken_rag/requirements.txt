# Core — local embeddings and vector math
sentence-transformers>=3.0
numpy>=1.24

# Frontier providers (either is enough; install both for --provider auto)
anthropic>=0.40
openai>=1.60

# Web UI (optional — only needed for `python -m kraken_rag serve`)
fastapi>=0.115
uvicorn>=0.32
python-multipart>=0.0.9
